name: probe4
on: workflow_dispatch
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: doh and curl
        run: |
          set +e
          echo '=== DOH ==='
          curl -sS -m 20 'https://cloudflare-dns.com/dns-query?name=neuqprobemtr0oeou.workers.dev&type=A' -H 'accept: application/dns-json'
          echo ''
          echo '=== DIRECT ==='
          curl -sS -m 45 https://neuqprobemtr0oeou.workers.dev/probe-neuq
          echo ''
          echo '=== END ==='
